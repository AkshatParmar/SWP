import * as React from 'react';
import {
    StyleSheet,
    TouchableOpacity,
    Text,
    Image,
    View
  } from 'react-native';
export default function App(){
  
    return (
        <Text style={{color: '#f04b48', fontSize: 40, fontWeight: 'bold', textAlign: 'center', marginBottom: 20}}>Got it
        </Text>
    );
  
}